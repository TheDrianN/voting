version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install
    build:
      - npx prisma generate
      - npm run build
    post-build:
      - echo "Compilación completada"
run:
  command: npm run start:prod
  network:
    port: 3000
  env:
      - name: PORT_MC
        value: "3005"
      - name: DATABASE_URL
        value: "mysql://admin:cip20242@bdcip.c16mgawm2sxa.us-east-2.rds.amazonaws.com:3306/bdcip_votes?schema=public"
